version: '3'
services:
  db:
    image: mysql:8.0.28
    cap_add:
      - SYS_NICE
    restart: always
    environment:
      - MYSQL_DATABASE=test_db
      - MYSQL_ROOT_PASSWORD=root
    ports:
      - '3307:3306'
    volumes:
      - db-data:/var/lib/mysql
volumes:
  db-data:
